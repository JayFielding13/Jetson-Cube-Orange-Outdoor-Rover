teleop_twist_joy_node:
  ros__parameters:
    # Left stick vertical (up/down) - Forward/Backward
    axis_linear:
      x: 1
    scale_linear:
      x: 0.7
    scale_linear_turbo:
      x: 1.5

    # Left stick horizontal (left/right) - Rotation
    axis_angular:
      yaw: 0
    scale_angular:
      yaw: 2.5  # Increased to match real robot rotation speed
    scale_angular_turbo:
      yaw: 3.5

    # No deadman switch - always enabled
    enable_button: -1  # -1 means no button required
    enable_turbo_button: 5  # Right bumper for turbo (optional)

    # Invert axes if needed
    inverted_reverse: false
    require_enable_button: false  # Disable deadman requirement
